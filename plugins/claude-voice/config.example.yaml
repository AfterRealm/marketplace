# claude-voice configuration
# Copy to `config.yaml` in the plugin root and customize. All fields optional.

# Hotkey to hold-to-talk. pynput chord notation — every key in angle brackets.
# Default is F8: single key, rarely conflicts. Chord combos are fine too, but
# Ctrl+Shift+Space in particular can clash with IMEs and Discord push-to-talk.
# Examples:
#   "<f8>"                          (recommended)
#   "<f9>"
#   "<ctrl>+<shift>+<space>"
#   "<ctrl>+<alt>+<v>"
hotkey: "<f8>"

# Language code for transcription. null = auto-detect.
# ISO codes: "en" english, "fr" french, "es" spanish, "de" german, "ja" japanese, "zh" chinese, ...
language: null

# Whisper model size. Bigger = more accurate, slower, more RAM.
#   tiny      ~ 75 MB,  fastest,   rough
#   base      ~150 MB,  fast,      OK (English-leaning)
#   small     ~500 MB,  balanced,  good (recommended)
#   medium    ~1.5 GB,  slow,      great
#   large-v3  ~3 GB,    slowest,   best (needs GPU to be usable)
whisper_model: "small"

# Inference backend. Use "cuda" + "float16" if you have an NVIDIA GPU.
whisper_device: "cpu"
whisper_compute: "int8"

# Audio
samplerate: 16000

# Hard cap on a single hold-to-talk clip, in seconds. Protects against runaway
# memory + silent Whisper stalls when the hotkey gets stuck or you walk away
# mid-hold. Uncapped audio at 16 kHz float32 is roughly 3.8 MB per minute.
# Raise for dictation (e.g. 300 = 5 min), lower for twitchy voice commands.
max_record_seconds: 60

# Press Enter after pasting the transcript (auto-send the message).
auto_send: true

# Focus safety — only paste if the focused window title contains this substring
# (case-insensitive). Set to "" to disable and paste into any focused window.
target_window: "claude"

# Log the transcribed text to the voice-mode terminal for review.
print_transcript: true

# ─── Mic sensitivity (for quiet / whispered speech) ───────────────────────

# Preset bundle for quiet or whispered speech. When true, claude-voice:
#   - boosts mic_gain to at least 4.0x (if you haven't set it higher)
#   - raises normalize_target_peak to 0.95
#   - loosens the silero VAD threshold to 0.15 (still blocks pure silence,
#     but lets whispers through — prevents hallucinations on accidental hotkey bumps)
#   - lowers Whisper's no-speech threshold to 0.2 so quiet frames aren't dropped
#   - disables previous-text conditioning (prevents hallucinated fill-in)
#   - primes Whisper with "The following contains quiet or whispered speech."
# Turn on if you tend to speak quietly or whisper when recording.
whisper_mode: false

# Manual pre-amp multiplier for quiet mics. Applied BEFORE auto-normalize.
# Valid range: 0.1 - 20.0. 1.0 = off. Try 2.0-3.0 for a quiet built-in laptop mic.
mic_gain: 1.0

# Automatically peak-normalize recordings toward normalize_target_peak.
# Boosts quiet audio AND rolls back anything that mic_gain pushed past 1.0
# (so hot gain on a merely-quiet source doesn't hard-clip). Safe default: on.
auto_normalize: true

# Target peak (0.0 - 1.0) after normalization. 0.8 leaves a safety margin.
# whisper_mode overrides this to 0.95 unless you've set it higher explicitly.
normalize_target_peak: 0.8
